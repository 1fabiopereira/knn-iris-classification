#include <set>
#include <map>
#include <vector>
#include <string>
#include "Element.h"

using namespace std;

class Classify {
	public:
		static string classification(vector<Element>& elements, Element& element, int K);
};
